# Stitch Reference Artifacts

This directory contains raw Stitch-exported reference artifacts.

Validation status:
- `triage_dashboard/`, `cve_workspace/`, and `news_intel/` were used as design reference inputs.
- `forensic_intelligence/DESIGN.md` is currently treated as style-system reference only and is not yet reconciled as approved product direction.

These files are retained for traceability and side-by-side comparison during UI iteration.
